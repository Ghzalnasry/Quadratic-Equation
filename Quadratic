import cmath

a = int(input("Enter the real num: "))
b = int(input("Enter the real num: "))
c = int(input("Enter the real num: "))
d = (b**2)-(4*a*c)
sol1 = (-b-cmath.sqrt(d))/(2*a)
sol2 = (-b+cmath.sqrt(d))/(2*a)
print(f"the solution are {sol1} and {sol2}")
